package model

const (
	HSET = "HSET"
	HGET = "HGET"
)

const (
	EXPIRE  = "EXPIRE"
	PERSIST = "PERSIST"
)

const (
	REPLICA = "REPLICA"
	DETAILS = "DETAILS"
	LOGS    = "LOGS"
)

const (
	REPLICA_OF = "REPLICA_OF"
)
